# 双栏 A4 排版示例：Typlatex 主题效果演示

张三，某大学某学院

> **摘要**：这是一份用于演示 Typlatex 主题的示例文档。正文以 A4 双栏
> 两端对齐排版，段落首行缩进两字符，标题自动编号，效果接近 LaTeX
> 期刊模板。将本文档在 Typora 中切换到 Typlatex 主题即可预览。

[TOC]

## 排版特性

正文使用衬线字体、五号字（10.5pt）、双栏两端对齐，段落首行缩进 2 字符，
章节标题后的第一段不缩进——这些都是 LaTeX 论文的典型习惯[^1]。

标题不自动编号；需要编号时直接写在标题文字里，如 `## 1. 引言`、
`### 1.1 方法`（想恢复 CSS 自动编号，见 typlatex.css 末尾注释）。

[^1]: 脚注同样可用，显示在文末。

### 小节示例

行内公式 $e^{i\pi} + 1 = 0$ 会留在栏内随文字排版；独立公式块则居中显示：

$$
\int_{-\infty}^{+\infty} e^{-x^2} \, \mathrm{d}x = \sqrt{\pi}
$$

### 图片规则

与文字同行的图片 ![](https://typora.io/img/logo.svg) 留在栏内；
而单独成段的图片会自动跨双栏居中显示，相当于 LaTeX 的 `figure*`：

![](https://picsum.photos/900/320)

![](https://picsum.photos/700/200)
*图 1.11　连写图注：图片下一行直接接整行斜体（不空行），整段跨栏、图注蓝色居中，与空行写法效果相同。*

想强制某张大图只占一栏时，用 HTML 块写
`<p class="nospan"><img src="..." style="width:95%"></p>` 即可。

## 表格与代码

表格默认留在栏内；表格上方放一条 `---` 分隔线即可跨栏显示（三线表风格）：

---

| 方法 | 准确率 | 参数量 | 训练时间 |
|:---------|------:|------:|--------:|
| Baseline | 81.2 | 25.6M | 4.2 h |
| + Ours   | **84.7** | 25.7M | 4.5 h |
| + Ours (large) | 86.1 | 63.4M | 9.8 h |

代码块留在栏内，长行自动换行：

```python
def greet(name: str) -> str:
    """示例代码块：等宽字体、浅灰底色。"""
    return f"Hello, {name}! 这是一段很长的示例代码，用于演示长行自动换行的效果。"
```

## 结语

- 列表、任务列表、引用、脚注、`==高亮==`、`行内代码` 均已适配
- 导出 PDF 即为 A4 双栏版式
- 字号、栏距、页边距等参数见 `typlatex.css` 顶部变量

## 附录：不当缩进的元素

### 列表内不缩进

1. 有序列表正常编号
2. 第二项
   - 嵌套无序列表
   - 另一项

> 引用块使用左侧竖线与灰色文字，类似 LaTeX 的 quote 环境。

---

以上，请切换主题后查看效果。这一段由上方的 `---` 分隔线触发跨双栏显示——分隔线的下一段自动跨栏，之后正文恢复双栏。

<span class="full"></span>这一段由段首隐形标记触发跨栏——不想露出分隔线时，把空标记贴在段首即可，标记本身不会显示，其后照常书写。
